sap.ui.define(["sap/ui/core/mvc/Controller"], function (Controller) {
	"use strict";
	return Controller.extend("z.zpkw_poreport.controller.V_POItem", {
		/**
		 * Called when a controller is instantiated and its View controls (if available) are already created.
		 * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.
		 * @memberOf z.zpkw_poreport.view.V_POItem
		 */
		onInit: function () {
			// Get the Router Info
			var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			// Validate/Match the Router Details sent from source using oRouter.navTo("Router_Detail", {SelectedItem: selectPO});
			oRouter.getRoute("Route_POItem").attachMatched(this._onRouteFound, this);
		},
		_onRouteFound: function (oEvt) {
				debugger;
				var oArgument = oEvt.getParameter("arguments");
				var sPO = oArgument.SelectedPO;
				var aFilters = [];
				aFilters.push(new sap.ui.model.Filter("Ebeln", sap.ui.model.FilterOperator.EQ, sPO));
				
				var oBinding = this.byId("it_item").getBinding("items");
				oBinding.filter(aFilters);
		},
				// var oItems = new sap.m.ColumnListItem({
		  //          cells:[
		  //          	new sap.m.Text({text: "{Ebeln}"}),
		  //          	new sap.m.Text({text: "{Ebelp}"}),
		  //          	new sap.m.Text({text: "{Menge}"}),
		  //          	new sap.m.Text({text: "{Matnr}"}),
		  //          	new sap.m.Text({text: "{Werks}"}),
		  //          	new sap.m.Text({text: "{Statu}"}),
		  //              ]
		  //      });
				
				// oBinding.bindAggregation("items", {
				//     path: "/POItemSet",
				//     template: oItems,
				//     filters: aFilter
				// });
				

		
			/**
			 * Similar to onAfterRendering, but this hook is invoked before the controller's View is re-rendered
			 * (NOT before the first rendering! onInit() is used for that one!).
			 * @memberOf z.zpkw_poreport.view.V_POItem
			 */
			//	onBeforeRendering: function() {
			//
			//	},
			/**
			 * Called when the View has been rendered (so its HTML is part of the document). Post-rendering manipulations of the HTML could be done here.
			 * This hook is the same one that SAPUI5 controls get after being rendered.
			 * @memberOf z.zpkw_poreport.view.V_POItem
			 */
			//	onAfterRendering: function() {
			//
			//	},
			/**
			 * Called when the Controller is destroyed. Use this one to free resources and finalize activities.
			 * @memberOf z.zpkw_poreport.view.V_POItem
			 */
			//	onExit: function() {
			//
			//	}
			
		/**
		 *@memberOf z.zpkw_poreport.controller.V_POItem
		 */
		GoToFirstView: function (oEvent) {
			//This code was generated by the layout editor.
			var oRouter = sap.ui.core.UIComponent.getRouterFor(this);
			oRouter.navTo("Route_POHeader");
		},
		/**
		 *@memberOf z.zpkw_poreport.controller.V_POItem
		 */
		OnProcessClick: function (oEvent) {
			//This code was generated by the layout editor.
		}
	});
});