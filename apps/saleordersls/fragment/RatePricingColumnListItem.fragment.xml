<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<ColumnListItem type="Inactive">
		<cells>
			<Text text="{global>Carrier}"/>
			<Text text="{global>Service}"/>
			<!--<Text text="{= parseInt(${global>Delivery}) }"/>-->
			<Text text="{global>Delivery}"/>
			<Text text="{global>Counter}"/>
			<Text text="{global>Kappl}"/>
			<Text text="{= parseInt(${global>Stunr}) }"/>
			<Text text="{global>Kschl}"/>
			<Text text="{= parseInt(${global>Stunb}) }"/>
			<Text text="{= parseInt(${global>Stun2}) }"/>
			<core:Icon src="{= ${global>Billrate} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Billrate} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${global>Updkz} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Updkz} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${global>Sumind} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Sumind} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${global>Userexit} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Userexit} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${global>Determ} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Determ} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${global>Wsrate} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${global>Wsrate} === true ? 'green': 'red' }"/>
			<Text text="{global>Ppaidaddrel}"/>
			<Text text="{global>Condupdate}"/>
			<Text text="{global>Baserate}"/>
			<Text text="{global>Rate}"/>
			<Text text="{global>Perc}"/>
			<Text text="{global>Currency}"/>
		</cells>
	</ColumnListItem>
</core:FragmentDefinition>