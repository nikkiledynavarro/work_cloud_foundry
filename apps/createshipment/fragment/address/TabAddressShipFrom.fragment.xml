<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form"
	xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1">
	<f:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12"
		adjustLabelSpan="false" emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="3" columnsL="2" columnsM="2"
		singleContainerFullSize="false">
		<f:content>
			<core:Title text="General Info"></core:Title>
			<Label text="Name/Attention"/>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Name" tooltip="Name"
				value="{local>/basic/partners/shipfrom/address/name1}"></Input>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Attention" tooltip="Attention"
				value="{local>/basic/partners/shipfrom/address/name2}"></Input>
			<Label text="Address 1/2"/>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Address 1" tooltip="Address 1"
				value="{local>/basic/partners/shipfrom/address/street1}"></Input>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Address 2" tooltip="Address 2"
				value="{local>/basic/partners/shipfrom/address/street2}"></Input>
			<Label text="Address 3/4"/>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Address 3" tooltip="Address 3"
				value="{local>/basic/partners/shipfrom/address/street3}"></Input>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Address 4" tooltip="Address 4"
				value="{local>/basic/partners/shipfrom/address/street4}"></Input>
			<Label text="Phone/Fax"/>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Phone" tooltip="Phone"
				value="{local>/basic/partners/shipfrom/address/telephone}"></Input>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" placeholder="Fax" tooltip="Fax"
				value="{local>/basic/partners/shipfrom/address/fax}"></Input>
			<Label text="Fed/State TaxID"/>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" tooltip="Fed TaxID" placeholder="Fed TaxID"
				value="{local>/basic/partners/shipfrom/fedtaxid}"></Input>
			<Input enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" tooltip="State TaxID" placeholder="State TaxID"
				value="{local>/basic/partners/shipfrom/statetaxid}"></Input>
			<core:Title text="Location"></core:Title>
			<Label text="Country"/>
			<ComboBox selectedKey="{local>/basic/partners/shipfrom/address/country}" tooltip="Country" change="onUpdateDomestic"
				enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" items="{path: '/xSERPERPxI_PCCNTR', length: 999}"
				app:nextUpdate="slShipFromState">
				<items>
					<core:Item text="{Name}" key="{Country}"/>
				</items>
			</ComboBox>
			<Label text="Region"></Label>
			<ComboBox id="slShipFromState" tooltip="State" change="onUpdateDomestic" selectedKey="{local>/basic/partners/shipfrom/address/state}"
				enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }" items="{path: '/xSERPERPxI_PCSTATE', length: 999}">
				<items>
					<core:Item text="{Description}" key="{Region}"/>
				</items>
			</ComboBox>
			<Label text="City"/>
			<Input placeholder="City" value="{local>/basic/partners/shipfrom/address/city}"
				enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }"></Input>
			<Label text="Postal"/>
			<Input placeholder="Postal Code" value="{local>/basic/partners/shipfrom/address/postalcode}"
				enabled="{= (${local>/basic} &amp;&amp; ${local>/CanShip} === true) ? true : false }"/>
		</f:content>
	</f:SimpleForm>
</core:FragmentDefinition>