{
    "name": "trackshipmentewm",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "ui5 build --config=ui5.yaml --clean-dest --dest dist",
        "package": "cd dist && bestzip trackshipmentewm.zip *",
        "start": "ui5 serve --config=ui5.yaml --open index.html",
        "test": "echo \"See test/testsuite.qunit.html \u2014 no headless runner wired (review-fix #5 item 7). Tests reachable manually via 'npm start' + browser navigation.\" && exit 0"
    },
    "devDependencies": {
        "@ui5/cli": "^3.11.0",
        "bestzip": "^2.2.1"
    },
    "ui5": {
        "dependencies": []
    }
}