<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form"
	xmlns:table="sap.ui.table">
	<Dialog title="Packing Overview" class="sapUiContentPadding" stretch="false">
		<table:TreeTable selectionMode="None"
			rows="{path:'local>/PackingOverviewList', parameters: {arrayNames:['Children'], numberOfExpandedLevels: 4} }" enableSelectAll="false"
			visibleRowCountMode="Interactive">
			<table:columns>
				<table:Column>
					<Label text="Identification"/>
					<table:template>
						<FlexBox direction="Column">
							<Label text="{path: 'local>node_desc', formatter: '.formatter.removeLeadingZero'}"
								design="{= ${local>state} !== undefined ? ${local>state} : 'Standard' }" visible="{= ${local>outermost} !== 'Success' ? true : false }"/>
							<ObjectNumber number="{path: 'local>node_desc', formatter: '.formatter.removeLeadingZero'}" state="{local>outermost}"
								visible="{= ${local>outermost} === 'Success' ? true : false }"/>
						</FlexBox>
					</table:template>
				</table:Column>
				<table:Column>
					<Label text="Material"/>
					<table:template>
						<Text text="{path: 'local>material', formatter: '.formatter.removeLeadingZero'}" wrapping="false" tooltip="{path: 'local>material'}"/>
					</table:template>
				</table:Column>
				<table:Column>
					<Label text="Quantity"/>
					<table:template>
						<Text text="{local>qty}" tooltip="{local>qty}"/>
					</table:template>
				</table:Column>
				<table:Column>
					<Label text="Unit"/>
					<table:template>
						<Text text="{local>unit}" tooltip="{local>unit}"/>
					</table:template>
				</table:Column>
			</table:columns>
		</table:TreeTable>
		<endButton>
			<Button press="onClosePackingOverviewDialog" text="Close"/>
		</endButton>
	</Dialog>
</core:FragmentDefinition>