<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<ColumnListItem type="Inactive">
		<cells>
			<Text text="{local>Carrier}"/>
			<Text text="{local>Service}"/>
			<Text text="{= parseInt(${local>Delivery}) }"/>
			<Text text="{local>Counter}"/>
			<Text text="{local>Kappl}"/>
			<Text text="{= parseInt(${local>Stunr}) }"/>
			<Text text="{local>Kschl}"/>
			<Text text="{= parseInt(${local>Stunb}) }"/>
			<Text text="{= parseInt(${local>Stun2}) }"/>
			<core:Icon src="{= ${local>Billrate} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Billrate} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${local>Updkz} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Updkz} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${local>Sumind} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Sumind} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${local>Userexit} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Userexit} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${local>Determ} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Determ} === true ? 'green': 'red' }"/>
			<core:Icon src="{= ${local>Wsrate} === true ? 'sap-icon://accept': 'sap-icon://decline' }"
				color="{= ${local>Wsrate} === true ? 'green': 'red' }"/>
			<Text text="{local>Ppaidaddrel}"/>
			<Text text="{local>Condupdate}"/>
			<Text text="{local>Baserate}"/>
			<Text text="{local>Rate}"/>
			<Text text="{local>Perc}"/>
			<Text text="{local>Currency}"/>
		</cells>
	</ColumnListItem>
</core:FragmentDefinition>